# Placeholder so hosted builders can lstat this path.
# Local SQLite — override with Postgres in the host env dashboard or `.env.local`.
DATABASE_URL="file:./clinic.db"
ADMIN_PASSWORD="changeme"
SESSION_SECRET="dev-session-secret-change-me"
