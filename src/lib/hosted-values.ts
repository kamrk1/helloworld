/** Filled at production build from hosted.json. Keep empty in git. */
export const HOSTED: {
  DATABASE_URL?: string;
  ADMIN_PASSWORD?: string;
  SESSION_SECRET?: string;
  PLATFORM_PASSWORD?: string;
} = {};
